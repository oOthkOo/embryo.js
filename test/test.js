var chai = require('chai')
var expect = chai.expect

describe('General', function() {
    describe('constructor', function() {
        it('should have a default name', function() {
        
            expect('').to.equal('')
        })
        it('should have a power score', function() {
      
            expect('').to.equal('')
        })
    })
})
